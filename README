All existing JavaScript frameworks focus on the UI.
The goal of groundjs is to extend JavaScript language itself.

We also plan to add 
- Java implementation of CommonJS
- Functions that will allow JavaScript to run on Java

You can take only the js files you need or one merged file ground.js
For production minify the files

The code is simple
